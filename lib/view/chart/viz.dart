import 'package:flutter/material.dart';

class VizTab extends StatelessWidget {
  const VizTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
